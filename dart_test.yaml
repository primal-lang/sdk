timeout: 120s

tags:
  compiler:
    description: "Compiler pipeline tests (reader, lexer, parser, semantic)"
  runtime:
    description: "Runtime execution tests (standard library, evaluation)"
  io:
    description: "I/O tests (file, directory, console, environment)"
  unit:
    description: "Unit tests (models, utils, extensions, platform)"
  cli:
    description: "CLI tests run on every desktop platform (entrypoint, dart:io platform layer, installer)"
  binary:
    description: "Smoke tests for the compiled binary, which must be built before they can run"
