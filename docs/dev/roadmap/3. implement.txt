i want to implement a new the feature in the language. the feature is documented in this file.

steps:
1. read the document carefully
2. create a very detailed plan and write that plan in a file in the root of the project. the plan should have checkboxes that you must check as you implement it. the plan should also include steps at the end to:
    - update the documenation in @docs/
    - add tests in all levels in @test/
3. proceed to implement the plan

constraints:
- you are forbidden to run the whole test suite
- you can only run individual tests or just a few (only for the ones you implemented in this session)
- i will run the whole test suite manually after you finish and inform you if there are any problems